%h2.upcase
  Galeria
.photos
  - @galleries.each do |gallery|
    .photo
      .element
        %a{:href => "#{gallery[:slideshow]}"}
          %img{:src => gallery[:thumbnail]}
      .member
        = gallery[:title]
